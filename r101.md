Ts method

// std = namespace of standard, basically everything in C++
// << is how you  display something to console. printf, print, etc
// cout = character output

# Ok we gettin into objects

"the key to performance is only to perform necessary work"

projects - collections of instructions that manipulate data for a desired result

mk so we have to initialize and use objects so that we dont try to access memory directly...
an object being a region of storage/memory that can hold a value
variables vs objects - OOOOOH WHAT variables are named objects... Yo...

// 1.4

int x;
default initialization - garbage value

int x = 5;
copy initialization - less efficient

int x ( 5 );
direct initialization - used when values are explicitly cast to other types, like static_cast, now superseded by direct-list-initialization:

// modern/preferred versions below

int x {};
value/zero initialization - avoid unless you intend to replace it and the object's value is temporary
otherwise, use uniform initialization when youre actually using the initial values
i.e. std::cin >> x;

int e { 9 }, f { 10 };
direct-list-initialization (preferred because the code doesnt compile if its a wrong initialization - i.e. it avoids narrowing conversions)
**list/uniform initialization - consistent and you can easily tell we're naming an obj. - int x { 5 };
so i think these are more efficient because C++ directly initializes the objects in their final memory space using the specified arguments
...whereas copy initialization creates a temporary object and then moves the copy object into the target object

examples
int w1 { 4.5 }; // compile error
int w2 = 4.5; // runs - but w2 is copy initialized to 4. and its just less efficient bc its creating that copy object
int a, b = 5; // BAD - a is not initialized to anything; you must initialize every variable itself
int a = 4, b = 5; // correct
\[\[maybe_unused]] double gravity { 9.8 }; - C++17 will not complain about these variables not being used :)






